[[plugins]]
	package = "@netlify/plugin-sitemap"
        [plugins.inputs]
        trailingSlash = true

[build.environment]
  NODE_VERSION = "18"
  NODE_OPTIONS = "--openssl-legacy-provider"

[functions]
  node_bundler = "esbuild"
  node_version = "18"